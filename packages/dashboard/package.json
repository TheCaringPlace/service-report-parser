{
  "name": "@thecaringplace/reporting-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf public/data dist",
    "dev": "npm run prebuild && vite",
    "prebuild": "mkdir -p public/data && cp ../parser/reports/*.json ./public/data/",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "chart.js": "^4.4.6",
    "history": "^5.3.0",
    "preact": "^10.29.1",
    "preact-router": "^4.1.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.5",
    "vite": "^8.0.10"
  }
}
